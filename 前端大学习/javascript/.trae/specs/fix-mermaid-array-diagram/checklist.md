# Checklist

- [x] L1084-1090 Mermaid 块中所有节点标签使用双引号包裹
- [x] 全文 14 个 Mermaid 块中无 `<br/>` 出现在未加引号的节点标签内
- [x] 全文 14 个 Mermaid 块中无 `=` 出现在未加引号的节点标签内（且非 `==` 比较）
- [x] 修改完成后原 Mermaid 流程图渲染无 Parse error

## 验证详情

通过 `Get-Content` + `Select-String` 检查全文所有 Mermaid 块的节点标签格式：

- **已修复的 4 个块**（含 `<br/>` / `=` / `delete` 等特殊符号）：
  - L20-27 JS 组成图
  - L1084-1090 数组操作图（用户报告的块）
  - L1578-1584 作用域图
  - L1865-1871 对象操作图
- **未变更的 10 个块**：已使用双引号或内容为简单中文/数字，无需修复。
