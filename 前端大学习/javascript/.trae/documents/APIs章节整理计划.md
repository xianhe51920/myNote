# APIs 章节整理计划

## Summary

读取 `e:\前端大学习\javascript\APIs\` 目录下 6 个章节文件夹（0.变量的声明 ~ 5.定时器-间歇函数）共 44 张网课截图，把"Web APIs 第一天（DOM-获取元素、修改属性）"的知识点整理到 `e:\前端大学习\javascript\javascript.md` 的 `## APIs` 节点下（位于 L2091）。

## Current State Analysis

- **`javascript.md` L2091** 当前只有一行 `## APIs`（位于 L2090 `---` 之后），后续为空。
- **`APIs/` 目录** 共 6 个章节文件夹 + 1 个 `api课程安排` 介绍页（50 张图片）：

  | # | 文件夹 | 章节标题 | 副标题（来自 0.png） | 图片数 |
  |---|---|---|---|---|
  | 0 | 0.变量的声明 | 课前铺垫：const 优先 | 变量声明有三个 var let const | 1-7（7 张） |
  | 1 | 1.web API 基本认知 | Web API 基本认知 | 1.1 作用和分类 / 1.2 什么是DOM / 1.3 DOM树 / 1.4 DOM对象 | 0-6（7 张） |
  | 2 | 2.获取DOM对象 | 获取DOM对象 | 2.1 根据CSS选择器获取（重点）/ 2.2 其他获取方法（了解） | 0-7（8 张） |
  | 3 | 3.操作元素内容 | 操作元素内容 | 3.1 innerText / 3.2 innerHTML | 0-4（5 张） |
  | 4 | 4.操作元素属性 | 操作元素属性 | 4.1 常用属性 / 4.2 样式属性 / 4.3 表单元素属性 / 4.4 自定义属性 | 0-11（12 张） |
  | 5 | 5.定时器-间歇函数 | 定时器-间歇函数 | 5.1 定时器函数介绍 / 5.2 定时器函数基本使用 | 0-4（5 张） |

- **重要上下文**：`APIs/api课程安排/` 显示完整 7 天课程安排（DOM获取/事件基础/事件进阶/节点操作/BOM/正则/实战），但当前目录**只包含第一天的内容**（获取元素 + 修改属性 + 定时器）。后续 6 天文件夹尚未提供，本计划仅整理现有内容，预留扩展点。
- **课程风格延续**：与 `## JS 基础` 章节保持完全一致——三级标题（`### N. 章节名`）、引子、四级子节（`#### N.M 小节名`）、表格、代码块、Mermaid 流程图。
- **用户偏好**（来自 user_profile.md）：喜欢显式声明的清晰风格；ide 已安装 Markdown Preview Enhanced / Markdown Preview Mermaid Support。

## Proposed Changes

### 改动：在 `javascript.md` L2091 之后追加 `## APIs` 章节内容

**文件**：`e:\前端大学习\javascript\javascript.md`

**位置**：L2091 `## APIs` 之后紧接空白行（当前 L2091 是 `## APIs` 标题本身，下方无内容）。

**结构**：在 `## APIs` 之下追加 1 个引子 + 6 个 `###` 章节：

```markdown
## APIs

> 来源：黑马程序员 Web APIs 第一天网课截图整理（DOM-获取元素、修改属性）

### 0. 变量的声明（课前铺垫）
### 1. Web API 基本认知
### 2. 获取 DOM 对象
### 3. 操作元素内容
### 4. 操作元素属性
### 5. 定时器 - 间歇函数
```

**章节内部统一模板**（与 JS 基础章节完全一致）：
- 引子（`> 章节涵盖：...`）
- 子节标题：`#### N.M 小节名`
- 文字要点用无序列表
- 代码示例用 ` ```js ` 代码块
- 概念对比 / 分类列表用表格
- 流程 / 关系类图示（DOM 树、JS 组成）用 Mermaid `flowchart LR/TD`
- 课程介绍类（7 天安排、设计原则）保留为简表 / 文字描述

**章节内容规划**：

1. `### 0. 变量的声明（课前铺垫）`
   - 0.1 三个关键字：`var` / `let` / `const`
   - 0.2 选择建议：**const 优先**，理由（语义化好 / 实际开发中默认 / React 等框架基本 const）
   - 0.3 选择纠结时：先 const，发现需要改时再改 let

2. `### 1. Web API 基本认知`
   - 1.1 作用和分类：JS = ECMAScript + Web APIs（DOM + BOM），用 Mermaid 描述组成树
   - 1.2 什么是 DOM：文档对象模型（Document Object Model）
   - 1.3 DOM 树：用 Mermaid `flowchart TD` 描述 HTML 文档结构（html → head/body → …）
   - 1.4 DOM 对象：用 Mermaid 描述浏览器将标签封装为对象的过程

3. `### 2. 获取 DOM 对象`
   - 2.1 学习目标与提问
   - 2.2 根据 CSS 选择器获取（重点）：`querySelector` / `querySelectorAll` 语法 + 代码示例
   - 2.3 其他获取方法（了解）：`getElementById` / `getElementsByTagName` / `getElementsByClassName` 表格对比

4. `### 3. 操作元素内容`
   - 3.1 `对象.innerText`：纯文本，不解析标签
   - 3.2 `对象.innerHTML`：可解析 HTML 标签
   - 二者对比表格 + 代码示例

5. `### 4. 操作元素属性`
   - 4.1 常用属性：`对象.属性 = 值`（href / title / src 等）+ 代码示例
   - 4.2 样式属性：`对象.style.样式 = 值`（行内样式）
   - 4.3 操作 className / classList：`className` 覆盖 / `classList.add/remove/toggle` 增删切换
   - 4.4 表单元素属性：`value` / `type` / `checked` / `disabled` / `selected`
   - 4.5 自定义属性：`data-` 开头 + `dataset` 对象访问

6. `### 5. 定时器 - 间歇函数`
   - 5.1 定时器函数介绍：每间隔一段时间自动执行代码（场景：网页倒计时、京东秒杀）
   - 5.2 基本使用：`setInterval(函数, 间隔毫秒)` + `clearInterval(timer)` 停止
   - 5.3 案例：倒计时

**额外保留**（在 `### 1. Web API 基本认知` 之前或作为引子）：
- 7 天课程安排表格（来自 `api课程安排/0-3.png`），让读者了解整体进度。
- 课程设计原则（来自 `api课程安排/4.png`）：拒绝百科全书 / 每天一个综合案例 / 有些课堂练习自己写 / 注重实战。

**图示处理策略**：
- **JS 组成图**（JavaScript → ECMAScript / Web APIs → DOM / BOM）→ Mermaid `flowchart LR`
- **DOM 树**（html → head/body → 各子节点）→ Mermaid `flowchart TD`
- **7 天课程安排**（圆形图标）→ 简表（无法用 Mermaid 表达圆形视觉元素）
- **黑马 logo / 装饰元素** → 跳过
- **京东秒杀倒计时图** → 用文字描述场景，代码块给倒计时实现

## Assumptions & Decisions

1. **范围假设**：用户只提供了 6 个文件夹（0~5），全部对应"第一天"内容。**不臆测**后续 6 天（事件基础、节点操作、BOM、正则、实战）的内容——等用户提供截图后再追加。
2. **章节编号**：保留文件夹原始数字（0~5），与目录顺序一致；`### 0. 变量的声明` 作为课前铺垫放在最前。
3. **风格假设**：与 `## JS 基础` 完全一致（用户上一轮已认可该风格）。
4. **图示策略**：能用 Mermaid 表达的流程/树用 Mermaid；圆形/品牌 UI 用表格或文字简述。
5. **可扩展性**：本次只整理"第一天"内容，后续添加"第二天"等内容时，只需在 `## APIs` 之下追加 `### 6. ...` 章节，不修改已有内容。
6. **不嵌入图片**：按用户隐含偏好（参考 JS 基础章节），不内嵌 PNG 路径，只整理文字+图表化的知识。

## Implementation Steps

### Step 1：按章节读取所有 1.png~末尾.png
- 6 个文件夹按 0→5 顺序逐个读取（0.png 已在 Phase 1 抽样读取过，子节标题已确认）。
- 每读完一张，在内部汇总该页文字要点，**不进 context**；每读完一整章立刻统一写入。

### Step 2：按章节写入 `javascript.md`
- 在 `L2091 ## APIs` 之后用 Edit 工具插入新内容。
- 写入顺序：引子 → 7 天课程安排表 → 课程设计原则 → `### 0` → `### 1` → `### 2` → `### 3` → `### 4` → `### 5`。
- 每章用 `### N. 名称` 开头，子小节用 `#### N.M`。
- 每章结束空一行分隔。
- 复用 `javascript.md` 已有的 `flowchart` / 表格 / 代码块风格。

### Step 3：本地校验
- 用 Read 工具读取 `javascript.md` 的 `L2085~末尾` 区间，确认：
  1. `## APIs` 之下出现 6 个 `###` 章节
  2. 0.png 抽样显示的子节标题（1.1~1.4 / 2.1~2.2 / 3.1~3.2 / 4.1~4.4 / 5.1~5.2）全部有 `####` 对应
  3. Mermaid 代码块语法正确，节点 ID 唯一
  4. 与 `## JS 基础` 章节风格统一

## Verification

1. **完整性**：`javascript.md` 在 `## APIs` 之下出现 6 个 `###` 章节；每章子节数量与 0.png 显示一致（1.1~1.4 / 2.1~2.2 / 3.1~3.2 / 4.1~4.5 / 5.1~5.3）。
2. **可渲染性**：在 VS Code 中打开 `javascript.md`，Markdown Preview Enhanced / Mermaid Support 能正确渲染所有 Mermaid 图。
3. **风格一致**：与 `## JS 基础` 章节（1~10 章）保持一致——三级标题、引子、四级子节、表格、代码块、Mermaid 流程图。
4. **可扩展性**：在第 5 章后追加"事件基础 / 节点操作 / BOM / 正则 / 实战"内容时，只需在末尾追加 `### 6.` 起的新章节。
