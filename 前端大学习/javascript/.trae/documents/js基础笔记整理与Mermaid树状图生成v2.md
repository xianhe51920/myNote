# 10. 对象 章节笔记整理 + Mermaid 树状图生成

## 1. Summary

本计划收尾 JS 基础笔记整理任务：

1. 将 `e:\前端大学习\javascript\JS基础\10.对象\` 全部 33 张图片（0-32.png）的内容整理追加到 `e:\前端大学习\javascript\javascript.md` 的 `### 10. 对象` 占位标题下，覆盖对象概念、声明、属性查改增删、对象中的方法、遍历对象、内置对象、堆栈内存分配等知识点。
2. 在 `e:\前端大学习\javascript\js基础Mermaid.md` 中生成 `## JS 基础` 为根节点、**向右展开**（`graph LR`）的 Mermaid 树状图，覆盖 10 个章节（1.JavaScript 介绍 ~ 10.对象）及其主要子节点。

## 2. Current State Analysis

### 2.1 `e:\前端大学习\javascript\javascript.md`（共 1569 行）

- **已完成章节**：
  - `### 1. JavaScript 介绍`
  - `### 2. 变量`（含变量拓展 - let 和 var 的区别、变量拓展 - 数组）
  - `### 3. 常量`
  - `### 4. 数据类型`
  - `### 5. 类型转换`
  - `### 6. 运算符`
  - `### 7. 语句`
  - `### 8. 数组`
  - `### 9. 函数`
- **占位但无内容**：第 2193 行仅有 `### 10. 对象` 一行标题，下方完全空白
- **图片引用约定**：相对路径 `JS基础/<子目录>/<图片名>.png`
- **格式约定**：三级标题分章、四级标题分小节、表格 + 代码块 + `**加粗**` + 图片引用

### 2.2 `e:\前端大学习\javascript\js基础Mermaid.md`

- 文件存在，内容为空，需填充完整 Mermaid 源码
- 用户 IDE 已安装 Markdown Preview Mermaid Support 插件，可直接渲染 ` ```mermaid ` 代码块

### 2.3 `e:\前端大学习\javascript\JS基础\10.对象\`（33 张 PNG）

- 0.png 章节标题
- 1.png 学习目标
- 2.png 目录：对象 / 综合案例
- 3.png 章节大纲：什么是对象 / 对象使用 / 遍历对象 / 内置对象
- 4.png 思考引入 - 数组 `[180,180,180]` 含义
- 5.png 对象是什么 - 概念与示例
- 6.png 总结 - 对象定义
- 7.png 对象声明语法 `let 对象名 = {}` / `new Object()`
- 8.png 对象有属性和方法 - 属性是名词，方法是动词
- 9.png 属性的概念与示例
- 10.png 总结 - 属性顺序、符号使用
- 11.png 增删改查总图
- 12.png 属性-查
- 13.png 属性-改
- 14.png 属性-增
- 15.png 属性-删
- 16.png 总结 - 查改增语法
- 17.png 属性-查的另外一种写法 `对象['属性']`
- 18.png 总结 - 访问属性两种方式
- 19.png 对象中的方法 - 声明
- 20.png 对象中的方法 - 调用
- 21.png 1.4 遍历对象 - 引入
- 22.png 遍历对象 - for in 语法
- 23.png 总结 - 遍历对象
- 24.png 1.5 内置对象 - 学习路径
- 25.png 内置对象是什么
- 26.png Math 对象常用方法
- 27.png 生成任意范围随机数
- 28.png 拓展 - 术语解释（关键字、保留字、标识符、表达式、语句）
- 29.png 拓展 - 基本数据类型和引用数据类型
- 30.png 堆栈空间分配区别
- 31.png 简单类型的内存分配
- 32.png 复杂类型的内存分配

## 3. Proposed Changes

### 3.1 续写 `javascript.md` 的 `### 10. 对象` 章节

- **起点**：第 2193 行 `### 10. 对象` 下方追加内容
- **文件**：`e:\前端大学习\javascript\javascript.md`
- **操作方式**：使用 `Edit` 工具在 `### 10. 对象` 标题后插入新内容
- **结构**：

```markdown
### 10. 对象

- 学习路径：
  - 什么是对象
  - 对象使用
  - 遍历对象
  - 内置对象

![对象章节导图](JS基础/10.对象/3.png)

#### 10.1 什么是对象

- 思考：`let arr = [180, 180, 180]` 中 180 含义不明
- 答案：**对象**（object）—— JavaScript 里的**一种数据类型**
- 特点：可以理解为是一种**无序的数据集合**，注意数组是有序的数据集合
- 作用：用来描述某个事物，例如描述一个人
  - 人有姓名、年龄、性别、还有吃饭睡觉打代码等功能
  - 如果用多个变量保存比较散，用对象比较统一
- 举例描述「班主任」信息：
  - 静态特征（姓名、年龄、身高、性别、爱好）→ 用数字、字符串、数组、布尔类型等表示
  - 动态行为（点名、唱、跳、rap）→ 用函数表示

![对象是什么 - pink 老师示例](JS基础/10.对象/5.png)

**总结：**

1. 对象是什么？
   - 对象是一种数据类型
   - 无序的数据的集合
2. 对象有什么特点？
   - 无序的数据的集合
   - 可以详细的描述描述某个事物

![对象总结](JS基础/10.对象/6.png)

#### 10.2 对象使用

- 目标：掌握对象语法，用它保存多个数据

**1) 声明语法**

```js
let 对象名 = {}
let 对象名 = new Object()
```

例如：

```js
// 声明了一个 person 的对象
let person = {}
```

- 实际开发中，我们多用**花括号**。`{}` 是**对象字面量**

![对象声明语法](JS基础/10.对象/7.png)

**2) 对象有属性和方法组成**

- **属性**：信息或叫特征（名词）。比如 手机尺寸、颜色、重量等
- **方法**：功能或叫行为（动词）。比如 手机打电话、发短信、玩游戏等

```js
let 对象名 = {
  属性名: 属性值,
  方法名: 函数
}
```

![对象有属性和方法](JS基础/10.对象/8.png)

**3) 属性**

- 数据描述性的信息称为属性，如人的姓名、身高、年龄、性别等，一般是名词性的。

```js
let obj = {
  uname: 'pink老师',
  age: 18,
  gender: '女'
}
```

- 属性都是成对出现的，包括属性名和值，它们之间使用英文 `:` 分隔
- 多个属性之间使用英文 `,` 分隔
- 属性就是依存在对象上的变量（外面是变量，对象内是属性）
- 属性名可以使用 `""` 或 `''`，一般情况下省略，除非名称遇到特殊符号如空格、中横线等

![属性的概念](JS基础/10.对象/9.png)

**总结：**

1. 对象属性有顺序吗？
   - 没有
2. 属性和值用什么符号隔开？多个属性用什么隔开？
   - 属性和值用 `:` 隔开
   - 多个属性用 `,` 隔开

![属性总结](JS基础/10.对象/10.png)

**4) 操作对象 - 增 删 改 查**

- 对象本质是无序的数据集合，操作数据无非就是 **增 删 改 查** 语法：

| 操作 | 说明 | 语法 |
| --- | --- | --- |
| **查** | 查询对象数据 | `对象.属性` |
| **改** | 重新赋值 | `对象.属性 = 值` |
| **增** | 对象添加新的数据 | `对象名.新属性名 = 新值` |
| **删** | 删除对象中属性 | `delete 对象名.属性` |

![增删改查总图](JS基础/10.对象/11.png)

**属性-查：**

- 声明对象，并添加了若干属性后，可以使用 `.` 获得对象中属性对应的值，称之为**属性访问**。
- 语法：`对象名.属性`
- 简单理解就是获得对象里面的属性值。

![属性-查](JS基础/10.对象/12.png)

**属性-改：**

- 语法：`对象名.属性 = 新值`

```js
let person = {
  uname: 'pink老师',
  age: 18,
  gender: '女'
}
person.gender = '男'
console.log(person.gender)  // 修改了为男
console.log(person)
```

![属性-改](JS基础/10.对象/13.png)

**属性-增：**

- 语法：`对象名.新属性 = 新值`

![属性-增](JS基础/10.对象/14.png)

**属性-删（了解）：**

- 语法：`delete 对象名.属性`

![属性-删](JS基础/10.对象/15.png)

**总结：**

1. 对象查语法如何写？
   - `对象名.属性`
2. 对象改语法如何写？
   - `对象名.属性 = 新值`
3. 对象增语法如何写？
   - `对象名.新属性名 = 新值`
4. 改和增语法一样，判断标准就是对象有没有这个属性，没有就是新增，有就是改

```js
let person = {
  uname: 'pink老师',
  age: 18,
  gender: '女'
}
console.log(person.age)       // 查语法
person.gender = '男'         // 改语法
person.address = '武汉黑马'  // 增语法
```

![增删改查总结](JS基础/10.对象/16.png)

**5) 属性-查的另外一种写法**

- 对于多词属性或则 `-` 等属性，点操作就不能用了。
- 我们可以采取：`对象['属性']` 方式，单引号和双引号都阔以

![属性-查的另外一种写法](JS基础/10.对象/17.png)

**总结：**

1. 对象访问属性有哪两种方式？
   - 点形式 `对象.属性`
   - `[]` 形式 `对象['属性']`
2. 两种方式有什么区别？
   - 点后面的属性名一定不要加引号
   - `[]` 里面的属性名一定加引号
   - 后期不同使用场景会用到不同的写法

![访问属性两种方式总结](JS基础/10.对象/18.png)

**6) 对象中的方法**

- 数据行为性的信息称为方法，如跑步、唱歌等，一般是动词性的，其本质是函数。

```js
let person = {
  name: 'andy',
  sayHi: function() {
    document.write('hi~~')
  }
}
```

- 方法是由方法名和函数两部分构成，它们之间使用 `:` 分隔
- 多个属性之间使用英文 `,` 分隔
- 方法是依存在对象中的函数
- 方法名可以使用 `""` 或 `''`，一般情况下省略，除非名称遇到特殊符号如空格、中横线等

![对象中的方法](JS基础/10.对象/19.png)

**方法调用：**

- 声明对象，并添加了若干方法后，可以使用 `.` 调用对象中函数，称之为**方法调用**。
- 也可以添加形参和实参

```js
let person = {
  name: 'andy',
  sayHi: function () {
    document.write('hi~~')
  }
}
// 对象名.方法名()
person.sayHi()
```

- 注意：千万别忘了给方法名后面加小括号

![对象中的方法-调用](JS基础/10.对象/20.png)

#### 10.3 遍历对象

- 目标：能够遍历输出对象里面的元素

**遍历对象的问题：**

- 对象没有像数组一样的 `length` 属性，所以无法确定长度
- 对象里面是无序的键值对，没有规律，不像数组里面有规律的下标

![遍历对象的引入](JS基础/10.对象/21.png)

**遍历对象：**

```js
let obj = {
  uname: 'andy',
  age: 18,
  sex: '男'
}
for (let k in obj) {
  console.log(k)      // 打印属性名
  console.log(obj[k]) // 打印属性值
}
```

- 一般不用这种方式遍历数组、主要是用来遍历对象
- `for in` 语法中的 `k` 是一个变量，在循环的过程中依次代表对象的属性名
- 由于 `k` 是变量，所以必须使用 `[]` 语法解析
- 一定记住：`k` 是获得对象的**属性名**，`对象名[k]` 是获得**属性值**

![for in 遍历对象](JS基础/10.对象/22.png)

**总结：**

1. 遍历对象用那个语句？
   - `for in`
2. 遍历对象中，`for k in obj`，获得对象属性是那个？获得值是那个？
   - 获得对象属性是 `k`
   - 获得对象值是 `obj[k]`

![遍历对象总结](JS基础/10.对象/23.png)

#### 10.4 内置对象

- 目标：学会调用 JavaScript 为我们准备好的内置对象
- 学习路径：
  1. 内置对象是什么
  2. 内置对象 Math
  3. 生成任意范围随机数

![内置对象学习路径](JS基础/10.对象/24.png)

**1) 内置对象是什么？**

- JavaScript 内部提供的对象，包含各种属性和方法给开发者调用
- 思考：我们之前用过内置对象吗？
  - `document.write()`
  - `console.log()`

![内置对象是什么](JS基础/10.对象/25.png)

**2) 内置对象-Math**

- 介绍：Math 对象是 JavaScript 提供的一个「**数学**」对象
- 作用：提供了一系列做数学运算的方法
- Math 对象包含的方法有：

| 方法 | 作用 |
| --- | --- |
| `random` | 生成 0-1 之间的随机数（包含 0 不包括 1） |
| `ceil` | 向上取整 |
| `floor` | 向下取整 |
| `max` | 找最大数 |
| `min` | 找最小数 |
| `pow` | 幂运算 |
| `abs` | 绝对值 |

- 官方文档：[Math 对象在线文档](https://developer.mozilla.org/zh-CN/docs/Web/JavaScript/Reference/Global_Objects/Math)

![Math 对象](JS基础/10.对象/26.png)

**3) 生成任意范围随机数**

- `Math.random()` 随机数函数，返回一个 0 - 1 之间，并且包括 0 不包括 1 的随机小数 `[0, 1)`
- 如何生成 0-10 的随机数呢？

```js
Math.floor(Math.random() * (10 + 1))
```

- 如何生成 5-10 的随机数？

```js
Math.floor(Math.random() * (5 + 1)) + 5
```

- 如何生成 N-M 之间的随机数

```js
Math.floor(Math.random() * (M - N + 1)) + N
```

![生成任意范围随机数](JS基础/10.对象/27.png)

#### 10.5 拓展 - 术语解释

- 目标：知道一些术语，让自己更专业

| 术语 | 解释 | 举例 |
| --- | --- | --- |
| 关键字 | 在 JavaScript 中有特殊意义的词汇 | `let`、`var`、`function`、`if`、`else`、`switch`、`case`、`break` |
| 保留字 | 在目前的 JavaScript 中没意义，但未来可能会具有特殊意义的词汇 | `int`、`short`、`long`、`char` |
| 标识（标识符） | 变量名、函数名的另一种叫法 | 无 |
| 表达式 | 能产生值的代码，一般配合运算符出现 | `10 + 3`、`age >= 18` |
| 语句 | 一段可执行的代码 | `if ()`、`for()` |

![术语解释](JS基础/10.对象/28.png)

#### 10.6 拓展 - 基本数据类型和引用数据类型

- 目标：了解基本数据类型和引用数据类型的存储方式
- 简单类型又叫做基本数据类型或者**值类型**，复杂类型又叫做**引用类型**。

**值类型（基本数据类型）：**

- 简单数据类型/基本数据类型，在存储时变量中存储的是值本身，因此叫做值类型
- 包括：`string`、`number`、`boolean`、`undefined`、`null`

**引用类型（复杂数据类型）：**

- 复杂数据类型，在存储时变量中存储的仅仅是地址（引用），因此叫做引用数据类型
- 通过 `new` 关键字创建的对象（系统对象、自定义对象），如 `Object`、`Array`、`Date` 等

![基本数据类型和引用数据类型](JS基础/10.对象/29.png)

**堆栈空间分配区别：**

1. **栈**（操作系统）：由操作系统自动分配释放存放函数的参数值、局部变量的值等。其操作方式类似于数据结构中的栈。
   - **简单数据类型存放到栈里面**
2. **堆**（操作系统）：存储复杂类型（对象），一般由程序员分配释放，若程序员不释放，由垃圾回收机制回收。
   - **引用数据类型存放到堆里面**

![堆栈空间分配区别](JS基础/10.对象/30.png)

**1) 简单类型的内存分配**

- 值类型（简单数据类型）：`string`、`number`、`boolean`、`undefined`、`null`
- 值类型变量的数据**直接存放在变量（栈空间）中**

```js
let usrAge = 12;     // 栈: 0x11 -> 12
let usrName = '小白'; // 栈: 0x20 -> 小白
let usrSex = 'true'; // 栈: 0x32 -> true
```

![简单类型的内存分配](JS基础/10.对象/31.png)

**2) 复杂类型的内存分配**

- 引用类型（复杂数据类型）：通过 `new` 关键字创建的对象（系统对象、自定义对象），如 `Object`、`Array`、`Date` 等
- 引用类型变量（栈空间）里存放的是**地址**，真正的对象实例存放在堆空间中

```js
let usrObj = {}
// 栈: 0x01 -> 0x22  (地址)
// 堆: 0x22 -> Object 对象实例
```

![复杂类型的内存分配](JS基础/10.对象/32.png)
```

### 3.2 生成 Mermaid 树状图到 `js基础Mermaid.md`

- **文件**：`e:\前端大学习\javascript\js基础Mermaid.md`
- **操作方式**：使用 `Write` 工具覆盖写入完整 Mermaid 源码
- **方向**：`graph LR`（左到右）
- **节点规范**：节点 ID 使用 ASCII 字母数字（`A`、`B`、`B1`），避免中文 ID
- **结构**：

```mermaid
# JS 基础 知识树状图

```mermaid
graph LR
  A[JS 基础]
  A --> B[1. JavaScript 介绍]
  A --> C[2. 变量]
  A --> D[3. 常量]
  A --> E[4. 数据类型]
  E --> E1[数字类型 Number]
  E --> E2[字符串类型 String]
  E --> E3[布尔类型 Boolean]
  E --> E4[未定义类型 undefined]
  E --> E5[空类型 null]
  A --> F[5. 类型转换]
  F --> F1[隐式转换]
  F --> F2[显式转换]
  A --> G[6. 运算符]
  G --> G1[赋值运算符]
  G --> G2[一元运算符]
  G --> G3[比较运算符]
  G --> G4[逻辑运算符]
  G --> G5[运算符优先级]
  A --> H[7. 语句]
  H --> H1[表达式和语句]
  H --> H2[分支语句]
  H --> H3[循环语句]
  A --> I[8. 数组]
  I --> I1[数组基本使用]
  I --> I2[操作数组 增删改查]
  I --> I3[冒泡排序]
  A --> J[9. 函数]
  J --> J1[函数声明与调用]
  J --> J2[函数传参与返回值]
  J --> J3[作用域与作用域链]
  J --> J4[匿名函数]
  A --> K[10. 对象]
  K --> K1[对象的声明与属性]
  K --> K2[对象的查改增删]
  K --> K3[遍历对象]
  K --> K4[内置对象 Math]
```
```

## 4. Assumptions & Decisions

- **不重写已有内容**：javascript.md 中已完成的 1-9 章节保持原样，仅在 `### 10. 对象` 标题下追加新内容
- **图片命名**：原样使用 `0.png` `1.png` 等文件名，不做重命名
- **Mermaid 节点命名**：使用 ASCII 字母数字 ID，节点文本可包含中文与空格（在 `[]` / `()` 内安全）
- **小节展开粒度**：4、5、6、7 章保留 2 级子节点（覆盖主要分类），8、9、10 章同样按主要小节展开，避免图形过于密集
- **格式一致**：沿用已有章节的格式（表格、代码块、加粗、图片引用）
- **不修改 `### 7-9` 章节**：根据当前探索结果，7-9 章节已正确写入；本次仅补 10.对象

## 5. Verification Steps

1. 重新打开 `javascript.md`，确认末尾包含完整的 `### 10. 对象` 章节，涵盖 10.1 ~ 10.6 六个子小节
2. 检查所有图片引用路径 `JS基础/10.对象/<n>.png`（0.png ~ 32.png）是否能在工作区打开（即文件真实存在）
3. 打开 `js基础Mermaid.md`，确认 Mermaid 源码以 ` ```mermaid ` 代码块包裹，根节点为 `A[JS 基础]`，包含 10 个一级章节
4. 在 VS Code 中使用 Markdown Preview Mermaid Support 预览，验证：
   - 树状图方向为从左到右（`graph LR`）
   - 10 个一级章节 + 各自子节点全部展示
   - 无 Mermaid 解析报错
