# JS 进阶 9-12 章节收尾整理计划

## 1. 任务目标
将 `e:\前端大学习\javascript\js进阶` 中新增的 4 个章节（9.深浅拷贝、10.异常处理、11.处理this、12.性能优化）整理为 `javascript.md` 中 `## JS 进阶` 下的 4 个三级章节，并同步更新 `js进阶Mermaid.md` 知识框架图，确保课程收尾完整、风格统一、内容准确。

## 2. 现状分析
- `e:\前端大学习\javascript\javascript.md`：总行数 4813，结尾为 `#### 8.7 本章总结`，`## JS 进阶` 包含 1~8 章节；2~8 章均已使用"三级标题 + 引子 + Mermaid 章节导览 + 四级子节 + 代码块 + 表格 + 本章总结表"的结构。
- `e:\前端大学习\javascript\js进阶Mermaid.md`：使用 Mermaid `graph LR` 树状图 + 章节子节统计表（合计 40 个子节）。需要为 I/J/K/L 4 个新章节分配节点 ID，并更新统计。
- `e:\前端大学习\javascript\js进阶\9.深浅拷贝\0.png` ~ `9.png` 已读取：浅拷贝（Object.assign/展开运算符/Array.concat）、深拷贝（递归/lodash.cloneDeep/JSON.stringify）。
- `e:\前端大学习\javascript\js进阶\10.异常处理\0.png` ~ `5.png` 已读取：throw 抛异常、try/catch/finally、debugger。
- `e:\前端大学习\javascript\js进阶\11.处理this\0.png` ~ `13.png` 已读取：this 指向（普通函数/箭头函数）、改变 this（call/apply/bind）。
- `e:\前端大学习\javascript\js进阶\12.性能优化\1.png` ~ `17.png` 已读取：防抖 debounce、节流 throttle、案例：视频播放位置记录。

## 3. 待新增章节结构

### 3.1 `### 9. 深浅拷贝`（引子 + 4 个子节 + 总结）
- `#### 9.1 浅拷贝`
- `#### 9.2 深拷贝`
- `#### 9.3 递归实现深拷贝`
- `#### 9.4 本章总结`

### 3.2 `### 10. 异常处理`（引子 + 4 个子节 + 总结）
- `#### 10.1 throw 抛异常`
- `#### 10.2 try / catch / finally 捕获异常`
- `#### 10.3 debugger 断点调试`
- `#### 10.4 本章总结`

### 3.3 `### 11. 处理 this`（引子 + 3 个子节 + 总结）
- `#### 11.1 this 指向`
- `#### 11.2 改变 this（call / apply / bind）`
- `#### 11.3 本章总结`

### 3.4 `### 12. 性能优化`（引子 + 4 个子节 + 总结）
- `#### 12.1 防抖（debounce）`
- `#### 12.2 节流（throttle）`
- `#### 12.3 案例 - 记录视频播放位置`
- `#### 12.4 本章总结`

子节合计：4 + 4 + 3 + 4 = **15**，原 40 + 新增 15 = **55**。

## 4. 实施步骤

### 步骤 1：追加 `### 9. 深浅拷贝` 到 `### 12. 性能优化` 到 `javascript.md`
- 工具：`Edit` 工具，在 `#### 8.7 本章总结` 表格的最后一行（`| `instanceof` | `obj instanceof Constructor` | 检测原型链中是否出现构造函数的 prototype |`）后追加。
- 每个三级章节包含：
  - 章节引子（来源、学习目标、章节导览 Mermaid 图）。
  - 子节使用 `#### N.M` 标题。
  - 关键概念用 `**加粗**` 标记。
  - 每个子节包含：目标 → 概念/语法 → 代码块（含 `// 注释`）→ Mermaid 流程图（必要时）→ 总结。
  - `#### N.M 本章总结` 用表格统一汇总。

### 步骤 2：更新 `js进阶Mermaid.md`
- 在 `graph LR` 中为 4 个新章节分配节点 `I`、`J`、`K`、`L`：
  - `JS --> I["9. 深浅拷贝"]`、`J["10. 异常处理"]`、`K["11. 处理 this"]`、`L["12. 性能优化"]`
  - 子节：`I1~I4`、`J1~J4`、`K1~K3`、`L1~L4`，按上文结构对应命名。
- 更新 "章节子节统计" 表格：新增 4 行，总子节数从 40 改为 55。

### 步骤 3：校验
- 工具：`Read` 与 `RunCommand`：
  - 校验 `javascript.md` 末尾新增段落完整、表格行数正确。
  - 校验 `#### ` 数量从原 40 增加到 55（多出 15 个）。
  - 校验 Mermaid 节点引用（无重复 ID、特殊字符已用双引号包裹）。
  - 校验新增章节引子、目标、代码块、Mermaid 图、总结表齐全。
  - 校验 `js进阶Mermaid.md` 中子节统计表合计为 55。

## 5. 风格与内容规范
- 与已完成章节严格保持一致：三级标题 `###`、四级标题 `####`、引子用 `> ...`、目标加粗、代码块用 ` ```javascript `、表格列固定 3 列以内。
- Mermaid 节点中含 `<br/>`、括号、空格的文本均使用 `"..."` 包裹。
- `Edit` 工具在原表格行后追加，所有新增内容一次写入（避免多次重复 edit）。
- `#### N.M` 编号与 `js进阶Mermaid.md` 中节点 ID 严格对应。

## 6. 风险与规避
- **风险**：原文件结尾为表格，`Edit` 时 `old_string` 唯一性。规避：使用表格最后一行（`| `instanceof` | `obj instanceof Constructor` | 检测原型链中是否出现构造函数的 prototype |`）作为锚点，定位准确且唯一。
- **风险**：图片数量多，信息量大导致总结偏差。规避：每节以"目标 + 关键概念 + 代码 + Mermaid + 总结"为骨架，仅提炼核心结论，避免冗余。
- **风险**：Mermaid 节点 ID 冲突。规避：原 A~H 已被使用 8 个一级节点，本计划用 I/J/K/L 顺延。
