# API 课程 14~19 章节整理计划

## Summary

将 `e:\前端大学习\javascript\APIs` 文件夹中**第 14~19 章**（日期对象、节点操作、M 端事件、JS 插件、windows 对象、本地存储 共 6 个子文件夹）按 11~13 章相同的标准（三级标题 + 引子 + 表格 + 代码块 + Mermaid）整理到 [javascript.md](file:///e:/%E5%89%8D%E7%AB%AF%E5%A4%A7%E5%AD%A6%E4%B9%A0/javascript/javascript.md) 的 `## APIs` 节点下（追加在 `### 13. 其它事件` 之后）。

## Current State Analysis

- **`javascript.md` 当前进度**：
  - `## APIs` 之下已包含 `### 课程导览` + `### 0.~13.` 共 15 个子节。
  - **`### 11. 事件流` ~ `### 13. 其它事件`** 已在前一轮整理完成（含 13.4 元素尺寸与位置 总结表）。
  - **本计划目标**：在 L3307 之后追加 `### 14.~19.` 共 6 个新章节。
- **`APIs\` 剩余 6 个子文件夹**（图片已全部读取完毕，内容已汇总）：
  - `14.日期对象`（1-7.png, 7 张；`0..png` 已跳过）
  - `15.节点操作`（0-12.png, 13 张）
  - `16.M端事件`（1.png, 1 张）
  - `17.JS插件`（1-2.png, 2 张）
  - `18.windows对象`（0-14.png, 15 张）
  - `19.本地存储`（0-12.png, 13 张）

## Proposed Changes

### 改动 1：在 `## APIs` 末尾追加 6 个新章节

**文件**：`e:\前端大学习\javascript\javascript.md`

**插入点**：在 `### 13. 其它事件` 末尾（L3307 之后）的"待补充"提示行**之后**追加（与之前 11~13 章末尾一致，附加一个分隔说明）。

**统一风格**（与 11~13 章完全一致）：
- `### N. 章节中文名`，下方加 `> 章节涵盖：...` 引子
- `#### N.M 小节名`
- 文字要点 → 无序列表
- 代码示例 → ` ```js ` / ` ```html ` 代码块
- 对比/分类 → 表格
- 流程/关系/树 → Mermaid `flowchart LR`，**节点文本中含 `<br/>` / `=` / `delete` 等特殊字符时必须用双引号包裹**（沿用上轮 fix-mermaid 规则）
- 装饰型图示（思考图标、浏览器截图、UI 截图、JS 插件目录列表）→ 1~2 句文字描述

**6 个新章节内容规划**：

#### 14. 日期对象
- 14.1 什么是日期对象（京东秒杀倒计时案例 + 学习路径 1.实例化 2.方法 3.时间戳 Mermaid）
- 14.2 实例化（`new Date()` 当前 / `new Date('2008-8-8')` 指定）
- 14.3 日期对象方法（getXxx 7 个方法表 + toLocaleString/DateString/TimeString 三种格式）
- 14.4 时间戳（1970-01-01 起算的毫秒数 + 倒计时算法 + 三种获取方式 `getTime` / `+new Date()` / `Date.now()` 表格对比）
- 14.5 本章总结

#### 15. 节点操作
- 15.1 DOM 节点（元素节点/属性节点/文本节点 + DOM 树 Mermaid）
- 15.2 查找节点（二维码关闭案例引出 + 父 `parentNode` / 子 `childNodes` vs `children` 重点 / 兄弟 `nextElementSibling` / `previousElementSibling` + 总结表）
- 15.3 增加节点（`document.createElement` + `parent.appendChild` / `insertBefore` + 克隆节点 `cloneNode(布尔值)`）
- 15.4 删除节点（`parent.removeChild(child)` + 隐藏 vs 删除的区别）
- 15.5 本章总结

#### 16. M 端事件
- 16.1 触屏事件（touchstart/touchmove/touchend 对比表）
- 16.2 本章总结（移动端事件与 PC 端区别，注意频繁触发性能问题）

#### 17. JS 插件
- 17.1 什么是插件（别人写好的代码，复制即可使用 + swiper 案例）
- 17.2 学习插件的基本过程（5 步：官网/演示/使用流程/API/类名区分）
- 17.3 本地文件结构（package/css/js 三个目录 + 官网链接）

#### 18. windows 对象
- 18.1 BOM（Browser Object Model + window 是顶级对象 Mermaid）
- 18.2 定时器-延时函数（`setTimeout(回调, 毫秒)` / `clearTimeout(timer)`）
- 18.3 JS 执行机制（单线程 → 同步 vs 异步 → 任务执行栈 vs 任务队列 → 事件循环）
- 18.4 location 对象（href/search/hash/reload + 跳转示例代码）
- 18.5 navigator 对象（userAgent + 检测 Android/iPhone 跳转移动站代码）
- 18.6 history 对象（back/forward/go + OA 办公系统案例）
- 18.7 本章总结

#### 19. 本地存储
- 19.1 本地存储介绍（数据存在用户浏览器，约 5M，刷新不丢失 + todomvc 案例）
- 19.2 本地存储分类（localStorage 永久 / sessionStorage 会话 对比表 + API `setItem/getItem/removeItem` + 浏览器查看 Application 面板）
- 19.3 存储复杂数据类型（`JSON.stringify` 存 / `JSON.parse` 取 + 应用面板变化）
- 19.4 本章总结（两种存储对比 + JSON 转换）

## Assumptions & Decisions

1. **跳过异常文件**：`14.日期对象\0..png`（双点）属于系统截图误生成，跳过该文件，从 `1.png` 开始。
2. **保留短章节**：`16.M端事件`（1 张）和 `17.JS插件`（2 张）虽然内容很少，仍独立成节以保持目录结构完整。
3. **Mermaid 节点文本引号规则**：节点文本中含 `<br/>` / `=` / `delete` 等特殊字符时必须用双引号包裹整段文本（沿用 `fix-mermaid-array-diagram` 规则）。
4. **风格一致性**：与 11~13 章完全相同的 `### N.` / `#### N.M` 编号系统、相同 Markdown 元素使用方式、相同 Mermaid `flowchart LR` 习惯。
5. **可扩展性**：本次只整理 14~19，未来新增章节（如 20.正则表达式）只需继续追加 `### 20. 章节名`，**不修改**已有内容。
6. **不内嵌 PNG 路径**：与之前一致，只整理文字+图表化的知识。

## Implementation Steps

### Step 1：在 `### 13. 其它事件` 末尾追加分隔提示 + 6 个新章节

- 用 `Edit` 工具在文件末尾的总结表（L3307 之后）追加：
  - 分隔提示（与 11~13 章之前一致）
  - `### 14. 日期对象` + 子节 14.1~14.5
  - `### 15. 节点操作` + 子节 15.1~15.5
  - `### 16. M 端事件` + 子节 16.1~16.2
  - `### 17. JS 插件` + 子节 17.1~17.3
  - `### 18. windows 对象` + 子节 18.1~18.7
  - `### 19. 本地存储` + 子节 19.1~19.4

### Step 2：本地校验

- 全文 `### ` 数量应为 22（课程导览 + 0~19 共 20 个 + 13 章内可能的小节）
- 全文 `#### ` 数量符合预期
- 全文 Mermaid 块节点文本已用双引号包裹（含 `<br/>` / `=` / `delete` 等特殊字符时）
- 再次 `Read` L3307~末尾 检查无格式错乱

## Verification

1. **完整性**：`## APIs` 之下出现 21 个 `###`（课程导览 + 0~19 共 20 个二级标题），其中本次新增 6 个。
2. **无渲染错误**：所有 Mermaid 块节点文本均用双引号包裹（含 `<br/>` / `=` / `delete` 等特殊字符时），不会再触发 parse error。
3. **风格一致**：与 11~13 章保持相同的三级标题 + 引子 + 表格 + 代码块 + Mermaid 风格。
4. **异常处理**：`14.日期对象\0..png` 已跳过，未引发错误。
